# link-checker
A web app for link safety analysis. Users can paste single or multiple URLs to check if they are safe or unsafe with clear indicators (green = safe, red = unsafe). Features include HTTPS verification, external link counting, scan history with date/time, suspicious word highlighting, and detailed results.
